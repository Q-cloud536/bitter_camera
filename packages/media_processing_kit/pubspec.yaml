name: media_processing_kit
description: "Part D blackbox for Flutter: media import/export pipeline with effect injection (Approach B)."
version: 0.0.1
homepage: "https://example.invalid"

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  ffmpeg_kit_flutter: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.bittercamera.media_processing_kit
        pluginClass: MediaProcessingKitPlugin
      ios:
        pluginClass: MediaProcessingKitPlugin

